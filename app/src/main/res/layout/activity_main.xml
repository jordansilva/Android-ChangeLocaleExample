<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.jordan.location.app.MainActivity">

    <Button
        android:id="@+id/btnPortugues"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="49dp"
        android:onClick="onClickPortugues"
        android:text="Português" />

    <Button
        android:id="@+id/btnIngles"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btnPortugues"
        android:layout_alignRight="@+id/btnPortugues"
        android:layout_below="@+id/btnPortugues"
        android:layout_marginTop="55dp"
        android:onClick="onClickIngles"
        android:text="Inglês" />

    <Button
        android:id="@+id/btnEspanhol"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btnIngles"
        android:layout_alignRight="@+id/btnIngles"
        android:layout_below="@+id/btnIngles"
        android:layout_marginTop="70dp"
        android:onClick="onClickEspanhol"
        android:text="Espanhol" />

</RelativeLayout>
